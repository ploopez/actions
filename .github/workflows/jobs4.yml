name: Configurar un workflow con dos Jobs (4)

on:
  workflow_dispatch:

jobs:
  crear_archivo:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Create a text file
      run: echo "This is a test file." > test.txt

  move_file:
    runs-on: ubuntu-latest
    needs: crear_archivo

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Move the text file
      run: |
        mkdir -p new_directory 
        mv test.txt new_directory/  

#Al ejecutar el workflow, cuando el segundo job se ejecuta, intenta mover test.txt
# sin embargo, este no tiene acceso al sistema de archivos creado por el primer job 
# resultará en un error que indica que el archivo no se encuentra
